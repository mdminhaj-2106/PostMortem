psycopg2-binary
python-dotenv
numpy
sentence-transformers
spacy
vaderSentiment

# after `pip install -r requirements.txt`, also run (needed transitively by
# Stage 6's own embedding_index.py, reached via stage6_bridge.py):
#   python -m spacy download en_core_web_sm
