# Simulator — Layer 2: Observed Sources

Deliberate degradation layer, built as SQL views over Layer 1, simulating fragmented real-world source systems. This is what the pipeline (Stage 1 onward) actually ingests.

See [`../README.md`](../README.md) for the full simulator spec and links to the source design docs.

No code yet.
