# Neon Postgres connection string — shared project so the whole team reads/writes
# the same simulator database instead of everyone generating their own local copy.
# Get this from the Neon dashboard: Project -> Connection Details -> "Connection string".
DATABASE_URL=postgresql://user:password@ep-xxxx.region.aws.neon.tech/dbname?sslmode=require
